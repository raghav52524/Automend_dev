# Gemini API key for dataset4 synthetic pipeline.
# Copy to .env and set your real key (do not commit .env).
# Or export in shell: set GEMINI_API_KEY=your-key-here  (Windows) / export GEMINI_API_KEY=your-key-here  (Linux/macOS)
GEMINI_API_KEY=dummy-replace-with-real-key

# Optional: Slack webhook for anomaly alerts. If set, pipeline will POST to this URL when anomalies are detected.
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL
